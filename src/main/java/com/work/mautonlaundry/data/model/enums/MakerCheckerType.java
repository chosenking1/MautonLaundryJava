package com.work.mautonlaundry.data.model.enums;

/** What a maker-checker request would hand over (Permission Architecture V2, spec §2.4). */
public enum MakerCheckerType {
    PERMISSION_ASSIGN,
    MODULE_ASSIGN,
    ROLE_ASSIGN
}
