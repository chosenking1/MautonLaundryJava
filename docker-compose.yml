services:

  backend:
    build: .

    container_name: imototo-backend

    restart: always

    ports:
      - "8079:8079"

    env_file:
      - .env

    extra_hosts:
      - "host.docker.internal:host-gateway"

    volumes:
      - ./config:/apps/config